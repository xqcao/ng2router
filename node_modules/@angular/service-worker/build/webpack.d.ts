/**
 * Webpack plugin that generates a basic Angular service worker manifest.
 */
export declare class AngularServiceWorkerPlugin {
    manifestFile: string;
    manifestKey: string;
    constructor(manifestFile?: string, manifestKey?: string);
    apply(compiler: any): void;
}
